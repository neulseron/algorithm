#include <iostream>

using namespace std;

/************************************************/
/*SWEA 1926 : 간단한 369게임					*/
/* solution : 자릿수 나누기						*/
/************************************************/

int main(int argc, char** argv)
{
	int maxNumber;
	cin >> maxNumber;
	for (int index = 1; index <= maxNumber; ++index)
	{
		int number100 = 0, number10 = 0 , number1 = 0, count369 = 0;
		number100 = index / 100;
		number10 = (index % 100) / 10;
		number1 = index % 10;
		if (number100 != 0 && number100 % 3 == 0)
		{
			++count369;
		}
		if (number10 != 0 && number10 % 3 == 0)
		{
			++count369;
		}
		if (number1 != 0 && number1 % 3 == 0)
		{
			++count369;
		}
		if (count369 == 0)
		{
			cout << index;
		}
		else
		{
			for (int count = 0; count < count369; ++count)
			{
				cout << '-';
			}
		}
		cout << ' ';
	}
	return 0;
}
